import React, { ReactElement, useState } from "react";
import styles from "./styles";

const Exercise = (): ReactElement => {
  return <div css={styles.wrapper}>exercise</div>;
};

export default Exercise;
